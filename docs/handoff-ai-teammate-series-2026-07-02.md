# 개발 핸드오프 — "AI 팀원 만들기" 3부작 묶기 (2026-07-02)

> ## 🗣 쉬운 요약
> - **무엇**: 흩어져 있는 AI 팀원 글 3편(기획자·QA·개발자)을 "하나의 시리즈"로 묶습니다.
> - **왜**: 지금은 태그로만 묶여 순서·맥락이 없습니다. 포트폴리오에서 "AI 팀을 하나의 시스템으로 만들었다"는 서사가 훨씬 강해지도록 의도된 묶음으로 만듭니다.
> - **그래서**: (A) 각 글 상단에 시리즈 안내 박스, (B) 전용 시리즈 페이지, (C) 겸사겸사 "3종"→"3인" 문구 수정. 실제 구현은 개발 세션 재량.

> 이 글 3편은 순서가 있다: **① 기획자 → ② QA → ③ 개발자** (작성일 01:00 / 01:06 / 01:12 순과 동일).

---

## 📋 개발 핸드오프

### A. 시리즈 안내 박스 (각 글 상단)

- **무엇을**: 3편 각각의 상단(리드 인용 바로 아래)에, 이 글이 "AI 팀원 만들기" 시리즈의 몇 번째인지 보여주고 나머지 편으로 이동하는 안내 박스를 넣는다.
- **수용 기준**:
  - [ ] 3편 모두 상단에 같은 형식의 시리즈 박스가 있다.
  - [ ] 박스에 **① 기획자 → ② QA → ③ 개발자** 순서가 보이고, **현재 글이 강조**된다.
  - [ ] 나머지 두 편으로 가는 링크가 동작한다(현재 글은 링크 아님).
  - [ ] 기존 디자인 토큰/톤만 사용(새 색·라이브러리 없음), 다크/라이트 정상.
- **영향 범위**: `ai-planner-teammate.md`, `ai-qa-engineer-teammate.md`, `ai-developer-teammate.md` 상단. 필요 시 재사용 컴포넌트 1개.
- **제약**: 본문 내용은 안 건드림(박스만 추가). 세 박스의 순서·라벨 일관.
- **참고 위치**: 세 글 파일 + `src/components/` (재사용 컴포넌트 패턴), `src/styles/`.

### B. 전용 시리즈 페이지

- **무엇을**: 3부작을 하나의 시스템으로 소개하는 큐레이션 페이지를 만든다. 팀을 왜 셋으로 나눴는지 + 순서대로 3편 링크 + 팀 구조를 한눈에.
- **수용 기준**:
  - [ ] "AI 팀원 만들기" 전용 페이지가 있고, **세 편을 순서대로** 소개·링크한다.
  - [ ] 팀 구조가 한 줄로 읽힌다 — **기획자(무엇을) · QA(무엇이 깨질까) · 개발자(어떻게)**, 그리고 셋이 핸드오프로 연결되고 서로 검수한다는 점.
  - [ ] 이 페이지로 오는 진입 동선이 있다(최소: 시리즈 박스에서 링크. 선택: nav 또는 홈에서).
  - [ ] 블로그 정체성 준수 — 과장 없음, 쉬운 설명, 평서체.
  - [ ] 빌드 green, 다국어(ko 기본) 라우팅 정상.
- **영향 범위**: 신규 페이지 1개(`src/pages/` 하위, 라우트는 dev 재량 — 예: `/series/ai-teammate`). 진입 링크 지점.
- **제약**: 각 글 본문은 안 건드림. 새 라이브러리 없이 기존 컴포넌트/토큰으로.
- **참고 위치**: `src/pages/` (기존 페이지 라우팅·i18n 패턴), `src/components/Card.astro`(카드 톤), 세 글의 frontmatter.

### C. (겸사) "3종" → "3인" 문구 수정

- **무엇을**: hero 성과 스트립의 "AI 팀원 **3종**"이 어색하다(종=물건 세는 말). **"3인"**으로 바꾼다.
- **수용 기준**:
  - [ ] 스트립 지표가 "**3인** / AI 팀원 (기획자·QA·개발자)"로 표시된다.
- **영향 범위**: `src/pages/index.astro` 의 stats 배열 1줄.
- **제약**: 다른 지표·레이아웃 무관.
- **참고 위치**: `src/pages/index.astro`.

---

## 검증 (개발자 규칙)

- 시리즈 박스: 3편 모두 렌더 확인, 링크 클릭 동작, 순서·강조 일관.
- 전용 페이지: 라이브에서 3편 링크 동작 + 진입 동선 확인.
- "3인": 배포본 스트립 실제 텍스트 확인.
- 완료 보고는 **돌린 확인 + 결과**로. "완료(검증됨)"만 인정.

---

## ✅ 구현 결과 (개발 세션, 2026-07-02)

**A. 시리즈 박스** — 3편 md의 리드 인용 바로 아래에 동일 형식 HTML 박스 삽입 (기존 토큰만: border-border/bg-muted/text-accent). 컴포넌트 대신 md 내 HTML을 택한 이유: md 본문에는 Astro 컴포넌트를 넣을 수 없고, MDX 전환은 "본문 안 건드림" 제약과 충돌 — 박스만 추가가 최소 변경.

**B. 시리즈 페이지** — `/series/ai-teammate` 신규 (`src/pages/series/ai-teammate.astro`). 인트로(왜 셋인가 + 핸드오프·상호 검수 한 줄) → 팀 구조 3카드(역할·질문) → "순서대로 읽기" Card 3장(getCollection에서 slug 순서 고정). 진입 동선 = 각 글 시리즈 박스의 "시리즈 소개 →" 링크.

**C. "3종"→"3인"** — index.astro stats 1줄 교체.

**돌린 확인 + 결과:**
- 빌드: `pnpm run build` → error/warn 0, `dist/series/ai-teammate/index.html` 생성 확인.
- 박스 존재: built HTML grep — 3편 각각 시리즈 박스 1개 + `/series/ai-teammate/` 링크 1개.
- 링크 타깃: 박스가 가리키는 3개 글 dist 경로 전부 존재.
- 현재 글 강조 = 링크 아님: 각 글 박스 범위에서 자기 자신 href 검색 → 3편 모두 0건.
- 순서: 시리즈 페이지 카드 href 순서 = planner → qa → developer (기획 명세와 일치).
- "3인": `dist/index.html` grep → "3인" 확인.
- 렌더: 프리뷰 서버 + 스크린샷 — 시리즈 페이지 전체, QA 편 박스(② 강조·①③ 링크·소개 링크) 눈검증.
- **라이트 모드: 확인함이 아니라 구조적 보장** — 시멘틱 토큰 클래스만 사용해 라이트 팔레트에 자동 매핑되지만, 라이트 픽셀 렌더는 headless에서 테마 토글이 안 돼 직접 보지 못함. 라이브에서 토글 1회 확인 권장.
