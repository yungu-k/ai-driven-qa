# 블로그 검토 & 개발 핸드오프 (2026-07-02)

> ## 🗣 쉬운 요약
> - **무엇**: ai-driven-qa 블로그의 글 16편 + 디자인을 검토한 결과와, 개발 세션이 바로 고칠 수 있는 수정 명세입니다.
> - **왜**: 이 블로그는 "과장 없는 담백한 실무 기록"이 정체성인데, 수치가 서로 어긋나거나 문체가 섞여 그 정체성을 스스로 깎는 지점이 있습니다.
> - **그래서**: 아래 수정 항목을 우선순위(🔴→⚪)로 정리했습니다. 각 항목은 "무엇을 / 이러면 완료(수용 기준) / 대상 파일"로 되어 있고, **구현 방법은 개발자 재량**입니다.

> 이 문서 사용법: 개발 세션을 `C:\dev\tech-blog` 폴더에서 열고, 저장소 규칙(README 등) + 이 문서를 읽고 착수하세요. 글 수정은 `src/content/posts/*.md`, `src/content/pages/about.md` 만 건드립니다.

---

## PART 1 — 글 수정

### 🔴 P0. 수치 신뢰성 (최우선 — 신뢰 직결)

- **무엇을**: 서로 어긋나거나 근거 없는 수치를 바로잡는다.
- **수용 기준**:
  - [ ] `repetition-to-ai-judgment-to-human.md` Phase 6 의 알람 건수를 **배경은 "약 600건", 결과는 "실측 615건"으로 명시**한다 (저자 확정 2026-07-02). 배경=어림, 결과=실측이라는 의미가 드러나게. 같은 데이터를 가리키는 두 표현임을 자연스럽게 연결.
  - [ ] `live-issue-triage.md` **제목의 "600건"이 본문에서 최소 1회 근거로 등장**한다 (현재 제목에만 있음).
  - [ ] `field-service-jira-sync.md` 와 `repetition…` Phase 7 의 **"100%"** 표현이 완화되거나 삭제된다 (본문상 현상/조치는 역동기화하지 않으므로 "100% 양방향"은 사실과 어긋남 → "필요 범위 전량 양방향" 등).
  - [ ] `kiosk-automation-infra.md` 의 "1,160 테스트 / 1,133 PASS / 0 fail"에서 **27건 차이(manual-required)가 수치로 설명**된다.
  - [ ] `kiosk-automation-infra.md` "32×4=128"의 **"약"이 삭제**된다 (정확히 떨어지는 곱셈).
  - [ ] "50건 넘으면"류가 **근거 병기되거나 "기본 페이지 한도를 넘으면"**으로 바뀐다.
- **영향 범위**: 위 4개 글 + 여정기. 수치 문구만.
- **제약**: 실제 값을 모르면 임의로 지어내지 말 것 — 불확실하면 "약/실측"으로 구분하거나 저자(공윤구)에게 확인.
- **참고 위치**: `src/content/posts/{repetition-to-ai-judgment-to-human,live-issue-triage,field-service-jira-sync,kiosk-automation-infra,qa-report-automation}.md`

### 🟡 P1. 문체·표기 통일

- **무엇을**: 한 글 안에서 문체를 통일하고, 표기 혼용을 정리한다.
- **수용 기준**:
  - [ ] `diff-driven-tc.md`, `live-issue-triage.md` 가 **한 글 안에서 존댓말(-습니다) / 평서체(-다) 중 하나로 통일**된다 (현재 배경=존댓말, 본문=평서체로 갈림).
  - [ ] 여정기가 전편 존댓말인 것이 **의도라면 그대로 두되**, 시리즈 문체 정책을 한 줄로 정한다 (다른 글은 평서체 유지 가능).
  - [ ] "릴리스 / 릴리즈" 혼용이 **한 표기로 통일**된다.
  - [ ] 마무리 경구("언어는 독자를 따른다", "자동화가 코드 품질을 끌어올린다", "같은 품질로 반복")가 **근거 범위 안으로 축소**된다.
- **영향 범위**: 대부분의 글. 어미·표기·마무리 문장.
- **제약**: 의미를 바꾸지 말 것. 문체·표기만.
- **참고 위치**: `src/content/posts/*.md`

### 🟡 P2. 영어 용어 풀이 (자기모순 해소)

- **무엇을**: "비개발자도 이해"를 표방하는 글에서 영어 용어에 우리말 풀이를 병기한다.
- **수용 기준**:
  - [ ] `catch-all`, `SSOT`, `Postmortem`, `KPT`, `off-by-one`, `룩앤필`, `월보드`, `Freeze Pane`, `제네릭` 등이 **각 글 첫 등장 시 우리말 병기**된다. 예: "월보드(벽에 거는 상황판)", "off-by-one(행 하나가 밀리는 오류)".
  - [ ] 특히 `live-issue-triage.md`·`global-incident-management.md` — "용어 평이화"를 교훈으로 내걸면서 영어 약어를 그대로 쓰는 **자기모순이 해소**된다.
- **영향 범위**: triage, incident, qa-report, tc-design, realtime-monitoring 등.
- **제약**: 용어 자체를 삭제할 필요는 없음. 첫 등장 풀이만.
- **참고 위치**: `src/content/posts/*.md`

### ⚪ P3. 도구명 실명 노출로 통일 (저자 확정 2026-07-02)

- **무엇을**: 본문에서 "이슈 트래커/디자인 도구"로 가린 부분을 **실제 도구명(Jira, Confluence, Figma 등)으로 노출**한다. 태그·slug 는 이미 실명이므로 본문을 거기에 맞춘다.
- **수용 기준**:
  - [ ] 본문의 익명 표현("이슈 트래커", "디자인 도구")이 **실제 도구명으로 교체**된다 (첫 등장에 정식명, 이후 축약 가능).
  - [ ] 본문 ↔ 태그/slug/소제목이 **일치**한다 (kiosk 글의 vitest/Playwright 실명 표기와 같은 결).
- **영향 범위**: bug-report, design-change-detection, field-service, global-incident 등.
- **제약**: 실제로 쓰는 도구명만 정확히. 사내 서버 주소·토큰 등 민감 정보는 노출 금지(도구 "이름"만).
- **참고 위치**: `src/content/posts/*.md` 의 본문 + frontmatter tags/slug

### ⚪ P4. 개별 must-fix

- **수용 기준**:
  - [ ] `regression-and-status-sync.md`: 제목 "양방향"인데 본문은 이슈→TC 단방향만 → **제목 수정** 또는 TC→이슈 방향 보강.
  - [ ] `global-incident-management.md`: 혼자 영어 제목 → **한글 병기**.
  - [ ] `repetition…` **Phase 5→6 전환 문장 추가** (내부 산출물 → 현장 운영으로 도메인이 점프하는 지점).
  - [ ] 번역투 정리: "초안하다"(diff-driven) → "초안을 잡으면", "…가 부담"(api-runner) → 서술어 복원.
- **참고 위치**: 해당 글 각각.

---

## PART 2 — 디자인 (정량 성과 스트립만)

> 나머지 디자인은 이미 트렌드에 부합(딥네이비 다크 + 인디고 액센트, hero pill, subtle 카드, Pretendard + keep-all) → **건드리지 않는다.**

### 🔴 D1. Hero 아래 정량 성과 스트립

- **무엇을**: 홈 hero 바로 아래에, 핵심 성과를 숫자로 보여주는 가로 스트립을 추가한다. (포트폴리오 임팩트를 즉시 각인)
- **수용 기준**:
  - [ ] hero 문단과 featured 섹션 **사이**에 3~4개 지표가 가로로 나열된다.
  - [ ] 지표 예: **"10년차 QA"** · **"자동화 1,100+ green"** · **"실전 기록 N편"**(N = 실제 글 수, **동적으로 산출** — 하드코딩 금지) · (선택) "AI 팀원 2종".
  - [ ] 모바일에서 세로로 접히거나 스크롤 없이 읽힌다(반응형).
  - [ ] 기존 디자인 토큰만 사용 — 새 색/폰트/라이브러리 추가 없음 (`--muted`, `--border`, `--accent`, Pretendard).
  - [ ] 다크/라이트 모드 모두 정상.
- **영향 범위**: `src/pages/index.astro` (hero `<section id="hero">` 직후). CSS는 기존 Tailwind 유틸 + 토큰.
- **제약**:
  - 수치는 **실제·방어 가능한 값만**. "1,100+"는 About 의 "1,100건 green baseline" 근거. 글 수는 컬렉션에서 동적으로.
  - 장식 과하게 하지 말 것(글래스모피즘·큰 이미지 금지). 현재의 절제된 톤 유지.
- **참고 위치**: `src/pages/index.astro` (hero 마크업), `src/styles/theme.css` (토큰), `src/components/Card.astro` (스타일 톤 참고).

---

## 다음 단계

이 문서를 개발 세션(`C:\dev\tech-blog`)에 넘기면, 위 수용 기준 체크리스트를 하나씩 만족시키며 수정 → 로컬 빌드 확인 → 커밋/배포하면 됩니다. 글 수정은 의미 보존이 원칙이고, 불확실한 수치는 저자 확인이 필요합니다.

---

## ✅ 구현 결과 (개발 세션, 2026-07-02)

- **P0 전항 완료** — 여정기 600(어림)↔615(실측) 연결, triage 본문에 근거 등장, "100%" 전량 완화(제목·본문·About, 소유권 서술로 대체), kiosk 1,160 = 1,133 PASS + 27 manual-required 명시, "약 128" → "= 128", "50건" → "기본 페이지 한도(50건)" (여정기·qa-report·getting-started 3곳).
- **P1 완료** — diff-driven 평서체 통일(triage는 재검토 결과 이미 평서체), "릴리즈"→"릴리스" 1건, 경구 3건 축소(diff-driven/kiosk/ai-planner). 문체 정책은 `docs/content-style.md` 신설.
- **P2 완료** — catch-all(포괄 수거함)·SSOT(Single Source of Truth)·Postmortem(사후 회고)·KPT(Keep/Problem/Try)·off-by-one(참조가 한 행씩 밀리는)·룩앤필(화면의 전체적인 생김새)·월보드(벽에 거는 상황판)·Freeze Pane(틀 고정)·제네릭(두루뭉술한 일반명)·ITIL(IT 서비스 관리 표준)·RLS(행 단위 접근 제어) 첫 등장 병기.
- **P3 완료** — "이슈 트래커"→Jira, "디자인 도구/툴"→Figma, "스프레드시트"→Google Sheets (본문·제목·description·표 전체). 잔여 grep 0건(가이드 글의 일반 지칭 1곳만 의도적 유지).
- **P4 완료** — regression 제목 "Jira 상태를 TC 시트로 자동 반영"으로 단방향 명시 + bug-report 링크로 왕복 관계 설명, incident 제목 한글 병기, 여정기 Phase 5→6 전환 문단 추가, 번역투 2건 교정.
- **D1 완료** — hero 직후 4지표 스트립(10년차 / 1,100+ / N편 동적 `sortedPosts.length` / AI 팀원 2종). 기존 토큰만 사용, 모바일 2×2 접힘, 빌드·렌더 검증 완료.
