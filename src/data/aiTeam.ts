/** AI 팀원 규모 — **여기 하나다.**
 *
 * 🔴 종전엔 수가 **두 자리에 글자로 박혀** 있었다: 홈 지표 「AI ×11」과 About 이력의
 * 「AI 팀원 5인」. 팀이 여섯에서 열하나로 늘 때 **홈만 고쳐지고 이력은 5인에 남았다.**
 * 채용이 또 있으면 같은 일이 난다 — `careerSpan.ts` 가 경력 길이를 한 곳으로 모은
 * 것과 같은 이유로 **수·역할·근거 링크를 여기서만 적는다.**
 *
 * 세는 축(2026-09-11 고정): **「직접 설계한 AI 팀원」 = 11.** 근거는 발행 글
 * `openfloor-team-dashboard` 의 명단 표라 **링크도 그 글로 건다.**
 * ⚠ 5부작 시리즈(`/series/ai-teammate/`)는 **원년 다섯**을 다루므로 이 수를 못 받친다
 * — 그 페이지의 「다섯」은 맞는 수이니 여기로 끌어오지 않는다.
 *
 * `roles` 는 **역할로 센 축**(8)이다. 같은 역할에 둘인 자리(개발·디자인·QA)가 있어
 * `size` 와 다르다 — 축을 섞으면 수가 갈린다.
 * 세션 이름(내부 식별자)은 공개 저장소에 안 내보낸다 — **역할 이름만** 쓴다.
 */
export const AI_TEAM = {
  size: 11,
  roles: ["기획", "PM", "개발", "QA", "디자인", "Technical Writer", "DevOps", "로그 분석"],
  href: "/posts/openfloor-team-dashboard/",
} as const;
